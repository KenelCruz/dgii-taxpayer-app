import { CurrencyDopPipe } from './currency-dop.pipe';

describe('CurrencyDopPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyDopPipe();
    expect(pipe).toBeTruthy();
  });
});
